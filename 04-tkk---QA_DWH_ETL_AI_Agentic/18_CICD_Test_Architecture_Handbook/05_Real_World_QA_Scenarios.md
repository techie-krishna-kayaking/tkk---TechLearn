# Real-World QA Scenario — Flaky Critical Gate

A critical reconciliation test flakes due to environment timing and is repeatedly rerun until green. Treat as release risk: isolate root cause, capture failure evidence, make dependencies/data deterministic and use a time-bound compensating control—not permanent ignore.
