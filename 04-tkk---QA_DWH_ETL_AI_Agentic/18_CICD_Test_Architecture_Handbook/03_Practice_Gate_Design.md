# Practice — Gate Design

Design gates for a release that changes ETL mapping, dashboard metric, LLM prompt and agent tool schema. Decide which checks run at PR, integration, pre-prod and post-prod; specify blockers and waiver authority.
