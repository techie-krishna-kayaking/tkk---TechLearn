# Real-World QA Scenario — PII Through Export

Dashboard masking works on screen, but CSV export returns raw email. Test UI, API, export, drill-through and scheduled delivery under multiple roles. Contain distribution, assess exposure, patch policy enforcement and add cross-channel regression coverage.
