# Practice — API Contract QA

Write a contract and test strategy for a customer-profile API that returns masked PII, supports pagination and publishes a change event. Include negative JSON, malicious pagination, expired token, duplicate update request and consumer schema-version tests.
