# Real-World QA Scenario — 200 With Missing Data

An API returns 200 after accepting a request, but downstream event processing fails. Use correlation IDs to reconcile acknowledgement, event, consumer result and persistence. Define asynchronous completion/SLA and alert rather than treating 200 as business success.
