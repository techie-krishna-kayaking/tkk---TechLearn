# ML Evaluation Cases

Test empty/null/out-of-range features; feature schema mismatch; point-in-time join leakage; label delay; deterministic repeatability; metric recomputation; threshold boundary; protected/critical slices; adversarial inputs; train-serve skew; drift alert and rollback/champion comparison.

Record dataset, feature definition, model/configuration, threshold, evaluator version and ground-truth cut-off with each result.
