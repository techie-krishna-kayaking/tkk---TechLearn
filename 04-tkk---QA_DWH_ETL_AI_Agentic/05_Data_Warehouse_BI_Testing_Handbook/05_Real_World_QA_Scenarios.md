# Real-World QA Scenario — Correct Fact, Wrong Dashboard

Warehouse fact totals reconcile, but a dashboard metric is high because a many-to-many semantic relationship duplicates rows. Validate metric grain, semantic query, relationships, filters and drill-through; lock the model with a semantic-layer regression test.
