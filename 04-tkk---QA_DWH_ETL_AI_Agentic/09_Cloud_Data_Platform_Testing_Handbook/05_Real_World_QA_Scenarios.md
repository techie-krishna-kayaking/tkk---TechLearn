# Real-World QA Scenario — Secret Expiry at Runtime

Warehouse credentials expire during a load. Verify safe failure, no partial publish, alert/audit evidence, rotation procedure, retry with new secret and final reconciliation. Confirm errors/logs do not reveal the secret or sensitive connection strings.
